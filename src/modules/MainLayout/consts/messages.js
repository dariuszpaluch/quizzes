import { defineMessages } from 'react-intl';

const messages = defineMessages({
  DRAWER_MENU_HEADER_TITLE: {
    id: 'drawer.menu.header-title',
    defaultMessage: 'Menu'
  }
});

export default messages;
